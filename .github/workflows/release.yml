name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
          cache: true
          cache-dependency-path: '**/packages.lock.json'

      - name: Install ILRepack tool
        run: dotnet tool install -g dotnet-ilrepack

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Test
        run: dotnet test --configuration Release --no-build --verbosity normal

      - name: Publish (ILRepack merges Domain + Infrastructure into TikTokExplode.dll)
        run: dotnet publish src/TikTokExplode/TikTokExplode.csproj --configuration Release --no-build --output publish

      - name: List publish files
        run: ls -la publish/

      - name: Create Release with single DLL
        run: |
          gh release create "${{ github.ref_name }}" \
            publish/TikTokExplode.dll \
            --generate-notes \
            --title "${{ github.ref_name }}" \
            --latest
        env:
          GH_TOKEN: ${{ github.token }}
